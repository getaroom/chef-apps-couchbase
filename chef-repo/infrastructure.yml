cookbooks:
- couchbase:
- apps-couchbase:
- chef_handler:
- minitest-handler:

environments:
- production:

roles:
- client:
- server:

data bags:
- apps:
  - pillowfight

nodes:
- ec2 1:
  - recipe[minitest-handler] role[client] role[server]
  - --no-host-key-verify -x ubuntu -G default -I ami-3c994355 -f c1.medium -d ubuntu12.04-gems --region us-east-1 -E production
